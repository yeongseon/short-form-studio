services:
  postgres:
    image: postgres:16.6-alpine
    ports:
      - "127.0.0.1:5432:5432"
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    restart: unless-stopped

  redis:
    image: redis:7.4-alpine
    ports:
      - "127.0.0.1:6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s
    restart: unless-stopped

  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    ports:
      # SECURITY: Bound to 127.0.0.1 — NEVER change to 0.0.0.0 in production.
      # The API has no TLS and relies on a reverse proxy for security.
      # See docs/SECURITY.md "Network Exposure Policy" for details.
      - "127.0.0.1:8000:8000"
    volumes:
      - ./data/artifacts:/app/data/artifacts
    env_file:
      - .env
    environment:
      PYTHONPATH: /app/src:/app
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

  worker:
    build:
      context: .
      dockerfile: apps/worker-orchestrator/Dockerfile
      target: runtime
    command: ["celery", "-A", "celery_app.celery_app", "worker", "--loglevel=info", "--pool=solo", "--concurrency=1"]
    volumes:
      - ./data/artifacts:/app/data/artifacts
    env_file:
      - .env
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

  # Celery Beat scheduler — required for any beat_schedule entry to actually fire.
  # Without this, reconcile_stale_dispatches / retry_failed_artifact_deletions never run (#585).
  beat:
    build:
      context: .
      dockerfile: apps/worker-orchestrator/Dockerfile
      target: runtime
    command: ["celery", "-A", "celery_app.celery_app", "beat", "--loglevel=info"]
    env_file:
      - .env
    depends_on:
      redis:
        condition: service_healthy
    restart: unless-stopped

  studio-web:
    build:
      context: .
      dockerfile: apps/studio-web/Dockerfile
      target: runtime
    ports:
      - "127.0.0.1:5174:8080"
    environment:
      API_KEY: ${API_KEY:-}
    depends_on:
      api:
        condition: service_started
    restart: unless-stopped

  ollama:
    image: ollama/ollama:0.6.2
    ports:
      - "127.0.0.1:11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    healthcheck:
      test: ["CMD", "ollama", "list"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    profiles:
      - gpu
    restart: unless-stopped

  stable-diffusion:
    image: short-form-studio-stable-diffusion:latest
    # Optional GPU profile service: requires a pre-built local image.
    # Dockerfile is expected at docker/stable-diffusion/Dockerfile once model code is available.
    # Example local build/tag: short-form-studio-stable-diffusion:1.0.0
    command: ["python", "launch.py", "--api", "--nowebui", "--listen", "--port", "7860", "--medvram", "--xformers", "--no-half", "--no-half-vae", "--skip-torch-cuda-test", "--skip-version-check", "--skip-python-version-check", "--skip-install"]
    ports:
      - "127.0.0.1:7860:7860"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    healthcheck:
      test: ["CMD-SHELL", "python3 -c \"import urllib.request; urllib.request.urlopen('http://localhost:7860/sdapi/v1/sd-models')\" || exit 1"]
      interval: 30s
      timeout: 30s
      retries: 5
      start_period: 120s
    profiles:
      - gpu
    restart: unless-stopped

  tts-qwen3:
    image: short-form-studio-tts-qwen3:latest
    # Optional GPU profile service: requires a pre-built local image.
    # Dockerfile is expected at docker/tts-qwen3/Dockerfile once model code is available.
    # Example local build/tag: short-form-studio-tts-qwen3:1.0.0
    ports:
      - "127.0.0.1:8100:8100"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    healthcheck:
      test: ["CMD-SHELL", "python3 -c \"import urllib.request; urllib.request.urlopen('http://localhost:8100/health')\" || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    profiles:
      - gpu
    restart: unless-stopped

  stt-whisper:
    image: short-form-studio-stt-whisper:latest
    # Optional GPU profile service: requires a pre-built local image.
    # Dockerfile is expected at docker/stt-whisper/Dockerfile once model code is available.
    # Example local build/tag: short-form-studio-stt-whisper:1.0.0
    ports:
      - "127.0.0.1:9000:8200"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    healthcheck:
      test: ["CMD-SHELL", "python3 -c \"import urllib.request; urllib.request.urlopen('http://localhost:8200/health')\" || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    profiles:
      - gpu
    restart: unless-stopped

  tts-cosyvoice:
    image: short-form-studio-tts-cosyvoice:latest
    # Optional GPU profile service: CosyVoice 3.0 (Fun-CosyVoice3-0.5B-2512).
    # Dockerfile is expected at docker/tts-cosyvoice/Dockerfile.
    # Model downloads automatically on first start from ModelScope/HuggingFace.
    # Example local build/tag: short-form-studio-tts-cosyvoice:1.0.0
    ports:
      - "127.0.0.1:50000:50000"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    environment:
      MODEL_DIR: /app/pretrained_models
      COSYVOICE_MODEL: Fun-CosyVoice3-0.5B-2512
    healthcheck:
      test: ["CMD-SHELL", "python3 -c \"import urllib.request; urllib.request.urlopen('http://localhost:50000/health')\" || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 120s
    profiles:
      - gpu
    restart: unless-stopped

  flower:
    image: mher/flower:2.0.1
    ports:
      - "127.0.0.1:5555:5555"
    environment:
      CELERY_BROKER_URL: redis://redis:6379/0
    depends_on:
      redis:
        condition: service_healthy
    profiles:
      - monitoring
    restart: unless-stopped

volumes:
  postgres_data:
  ollama_data:
